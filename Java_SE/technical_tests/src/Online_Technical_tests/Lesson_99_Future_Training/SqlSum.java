package Online_Technical_tests.Lesson_99_Future_Training;

/*  create table elements (
		v integer not null
	)							*/
// the answer is just the SQL sentence

public class SqlSum {
	String sqlQuery = new String("SELECT SUM(v) FROM elements");
}
